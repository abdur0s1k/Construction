<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обработка слов" Height="300" Width="500"
        Background="Black">

    <Window.Resources>
        <!-- Стиль для всех TextBlock -->
        <Style TargetType="TextBlock" x:Key="TurquoiseTextBlockStyle">
            <Setter Property="FontFamily" Value="Tahoma"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="Turquoise"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- Стиль для TextBox -->
        <Style TargetType="TextBox" x:Key="InputTextBoxStyle">
            <Setter Property="FontFamily" Value="Tahoma"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <!-- Стиль для Button -->
        <Style TargetType="Button" x:Key="TurquoiseButtonStyle">
            <Setter Property="Background" Value="Turquoise"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontFamily" Value="Tahoma"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontStyle" Value="Italic"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Введите слова через пробел:"
                   Style="{StaticResource TurquoiseTextBlockStyle}" />
        <TextBox x:Name="InputTextBox" Margin="0,5,0,10" Grid.Row="1"
                 Style="{StaticResource InputTextBoxStyle}"/>

        <TextBlock Text="Введите длину слова:"
                   Style="{StaticResource TurquoiseTextBlockStyle}" Grid.Row="2"/>
        <TextBox x:Name="LengthTextBox" Margin="0,5,0,10" Grid.Row="3"
                 Style="{StaticResource InputTextBoxStyle}"/>

        <StackPanel Orientation="Vertical" Grid.Row="4">
            <Button Content="Обработать" Click="ProcessButton_Click"
                    Style="{StaticResource TurquoiseButtonStyle}" />
            <TextBlock x:Name="ResultTextBlock" Margin="0,10,0,0"
                       Style="{StaticResource TurquoiseTextBlockStyle}"/>
        </StackPanel>
    </Grid>
</Window>
