<Window x:Class="WpfApp.ReverseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reverse Dialog" Height="200" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <StackPanel Margin="10" >

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10" VerticalAlignment="Center">
            <Label Content="Input:" Width="50"/>
            <TextBox Name="InputTextBox" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" Width="300"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Margin="0 0 0 10" >
            <CheckBox Content="Upper case" IsChecked="{Binding IsUpperCase}" Margin="0 0 20 0"/>
            <CheckBox Content="Reverse" IsChecked="{Binding IsReverse}" />
        </StackPanel>

        <Button Content="OK" Width="80" HorizontalAlignment="Right" Command="{Binding OkCommand}"/>
    </StackPanel>
</Window>
